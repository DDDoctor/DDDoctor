# 版本历史

> **发布版本：v1.0.5**  ·  整理日期：2026-09-12

---

## v1.0.5（本次发布版）

### 新增
- **截图 / 录制目录改为「相对程序目录」**（配置里存 `snapshots` / `recordings`）。
  换版本、挪文件夹时会自动跟随，不再写回旧版本目录。
- **载入旧配置时自动迁移**：把指向某个 `MultiView-*` 安装目录的绝对路径改回相对路径
  （用户手动指定过的位置，如 `E:\录像`，不受影响）。
- **画面标题栏显示声音状态**：🔊 绿色 = 在出声，🔇 灰色 = 静音。
- **工具栏「🎧 仅此路出声」**：只让当前通道出声，其余自动静音。
- **工具栏「🔇 当前通道静音」/ `Ctrl+M`**：单独切换某一路。
- 打包完整性校验工具 `tools/verify_build.py`。

### 修复
- 修复了历史遗留的"录制写到旧版本目录"问题（根因见下）。

---

## v1.0.4

### 修复
- **「配置 → 确定」卡死十几秒**（严重）：把 `video_set_aspect_ratio()` /
  `video_set_scale()` / `audio_set_*()` / `video_take_snapshot()` 也移出 UI 线程。
  这些接口内部都要抢 libvlc 的 **input 锁**，而后台的 `stop()` 会长时间持有它。
  实测 `_rebuild_wall()` 从 10 秒以上降到 **342 ms**（用户真实 NVR 地址）。
- **关闭程序后留下杀不掉的僵尸进程**：
  - 不再对卡住的线程调用 `QThread.terminate()`（Win32 `TerminateThread` 会让进程清理失败）；
  - 关闭时先隐藏窗口，再给后台一个收尾窗口；
  - 后台线程改用普通 Python **守护线程 + `queue.Queue`**，避免 `QThread` 对象
    在解释器退出时被析构而 Qt `qFatal`。

---

## v1.0.2

### 修复
- **双击画面"闪一下"但不全屏**：Qt 的鼠标事件在未被 `accept()` 时会向父控件冒泡，
  双击被 `_VideoSurface` 和 `VideoCell` 各处理一次（放大后立刻还原）。
  实测 `doubleClicked` 触发 2 次 → 修复后 1 次。

### 变更
- 双击改为**单画面全屏**（隐藏菜单栏/工具栏/状态栏，覆盖任务栏），
  再次双击或 `Esc` 还原；可在「配置 → 界面」里改回"只在窗口内放大"。
- 单击只触发一次（原来也是两次）。

---

## v1.0.1

### 修复
- 把 `stop()` / `set_media()` / `play()` / `release()` 移出 UI 线程
  （libvlc 3.x 没有 `stop_async`，只能靠后台线程隔离）。
  实测单路 `stop()` 最坏阻塞 **2002 ms**，修复后 **2 ms**。
- 修复每次起流时"陈旧的 `stopped` 事件"误触发一次无谓重连。
- 修复慢启动的流被连接超时误判为断流（缓冲中自动延长看门狗，上限 3×）。

### 新增
- 多路启动错峰（>6 路时每 250 ms 起一路），避免十几路同时初始化解码器造成 CPU 尖峰。
- 缓冲百分比刷新节流到 2 次/秒 + 单元格样式表缓存。
- 状态栏显示进程内存占用。
- 启动时间线日志（`程序启动 / VLC 运行库就绪 / 主窗口构建完成 / 进入事件循环`）。

---

## v1.0.0（初版）

- 最多 18 路通道任意搭配，自动网格铺满窗口
- 弹窗配置通道（启用 / 名称 / 流地址 / 音量 / 静音），批量粘贴导入
- 配置 JSON 保存 / 导入 / 导出
- 每路独立音量与静音、一键全局静音
- 双击单路放大 / `F11` 窗口全屏
- 断流自动重连（指数退避）+ 实时状态指示（边框颜色 + 文字）
- 截图、录制
- 打包为免安装 VLC 的 Windows x64 可执行程序（PyInstaller + 内置 `libvlc`）

---

## 附：几个关键缺陷的根因记录

| 现象 | 根因 | 定位手段 |
| --- | --- | --- |
| 点「配置 → 确定」卡死十几秒 | `libvlc_video_set_aspect_ratio()` 要抢 input 锁，被后台 `stop()` 长时间占住，主线程排队等锁 | `tools/monitor.py` 在卡死瞬间自动 `py-spy dump`，抓到主线程堆栈 |
| 双击闪一下不全屏 | Qt 鼠标事件未被 accept 时向父控件冒泡，同一次双击被处理两次 | `tools/dblclick_test.py` 统计信号触发次数 |
| 关闭后留下杀不掉的僵尸进程 | 对卡在 libvlc 里的线程调用 `QThread.terminate()`（= Win32 `TerminateThread`） | 对比"不等待/强杀/等收尾"三种策略的实测退出耗时 |
| 录制写进旧版本目录 | 早期把默认目录存成了绝对路径，换版本后仍指向旧目录 | 读配置文件发现路径里带 `MultiView-1.0.0-win64` |
| 多路里总有黑屏 | **不是程序 bug**：多路使用同一个 RTSP 地址，超出设备并发连接数被拒 | `tools/cells_probe.py` 逐路检查状态/vout + 实测 URL 可用性 |

详细现场数据见 `docs/卡死现场记录-py-spy.txt`。
